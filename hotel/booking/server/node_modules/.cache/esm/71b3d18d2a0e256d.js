let Hotel,fs,Order;_2a0‍.x([["create",()=>create],["hotels",()=>hotels],["image",()=>image],["sellerHotels",()=>sellerHotels],["remove",()=>remove],["read",()=>read],["update",()=>update],["userHotelBookings",()=>userHotelBookings],["searchListings",()=>searchListings]]);_2a0‍.w("../models/hotel",[["default",["Hotel"],function(v){Hotel=v}]]);_2a0‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_2a0‍.w("../models/order",[["default",["Order"],function(v){Order=v}]]);

    


       const create=async(req,res)=>{
    // console.log('req.fields',req.fields)
    //  console.log('req.files',req.files)
    try{
         let fields = req.fields
         let files = req.files 


         let hotel= new Hotel(fields);
         hotel.postedBy=req.user._id;
        //  handle image    
       if(files.image){
           hotel.image.data= fs.readFileSync(files.image.path);
           hotel.image.contentType=files.image.type;
       }
       hotel.save((err,result)=>{
           if(err){
               _2a0‍.g.console.log('saving hotel error=>',err)
               res.status(400).send("Error Saving");
           }
           res.json(result);
       })
    }catch(err){
        _2a0‍.g.console.log(err)
        res.status(400).json({
            err:err.message, 
        });
    };

};
       const hotels = async (req,res) =>{
    let all=await Hotel.find({}) 
    .limit(24)
    .select('-image.data')
    .populate('postedBy','_id name')
    .exec();
    // console.log(all)
    res.json(all);
   
   
}
       const image= async(req,res) =>{
    let hotel=await Hotel.findById(req.params.hotelId).exec()
    if(hotel && hotel.image && hotel.image.data!==null ){
        res.set('Content-Type',hotel.image.contentType)
        return res.send(hotel.image.data);
    }
};

       const sellerHotels= async(req,res)=>{
    let all=await Hotel.find({postedBy:req.user._id})
    .select('-image.data')
    .populate('postedBy','_id name')    
    .exec();
_2a0‍.g.console.log(all);
    res.send(all);
}

       const remove = async (req,res) =>{
    let removed = await Hotel.findByIdAndDelete(req.params.hotelId).select('-image.data').exec();
    res.json({removed});
};

       const read =async (req,res)=>{
    let hotel= await Hotel.findById(req.params.hotelId )
   
    .select('-image.data').exec()
    _2a0‍.g.console.log('SINGLE HOTEL',hotel)
    res.json(hotel);
};

       const update=async(req,res)=>{
    try{
       let fields=req.fields
       let files=req.files
       
       let data= {...fields};
       if(files.image){
           let image ={}
           image.data=fs.readFileSync(files.image.path)
           image.contentType=files.image.type;

           data.image=image;
       }

       let updated=await Hotel.findByIdAndUpdate(req.params.hotelId,data,{
           new:true
        }).select('-image.data');
       res.json(updated);
    }catch(err){
        _2a0‍.g.console.log(err)
        res.status(400).send('Hotel update failed. Try Again.')
    }
}
       const userHotelBookings= async (req,res) =>{
    const all =await Order.find({orderedBy:req.user._id})
    .select('session')
    .populate('hotel','-image.data')
    .populate('orderedBy','_id name')
    .exec();
    res.json(all);
}

       const searchListings = async (req,res)=>{
    const {location} = req.body;   
    
    let result= await Hotel.find({location}).select('-image.data').exec();
    res.json(result);
    
}